name: Refresh Phenix AI stack

on:
  push:
    branches:
      - fix/codex-sse-propagation

permissions:
  contents: write

jobs:
  refresh:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@93cb6efe18208431cddfb8368fd83d5badbf9bfd # v5
        with:
          ref: ${{ github.ref_name }}
      - uses: cachix/install-nix-action@a49548c11d9846ad46ecc0115273879b045f001c # v31
      - name: Refresh Phenix AI inputs
        run: nix flake update phenix-ai phenix-ai-nvim
      - name: Commit refreshed lock
        run: |
          if git diff --quiet -- flake.lock; then
            echo "flake.lock already current"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add flake.lock
          git commit -m "chore: refresh Phenix AI stack"
          git push origin HEAD:${GITHUB_REF_NAME}
